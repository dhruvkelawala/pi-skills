interface:
  display_name: "Herdr Hunk Walkthrough"
  short_description: "Guide a PR review in Hunk inside a Herdr split or tab"
  default_prompt: "Use $herdr-hunk-walkthrough to open the current PR in Hunk on Herdr and add a numbered code walkthrough. Add `tab` to open it in a new Herdr tab."

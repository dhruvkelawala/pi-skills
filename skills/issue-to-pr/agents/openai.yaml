interface:
  display_name: "Issue to PR"
  short_description: "Ship one GitHub issue as a reviewed, verified, gated PR"
  default_prompt: "Use $issue-to-pr to take this issue through implement, code review, verify, apr, and pr-watch into a merge-ready PR."

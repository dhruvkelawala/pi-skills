interface:
  display_name: "PR Watch"
  short_description: "Poll a PR until CI and review agents are clean"
  default_prompt: "Use $pr-watch to poll this PR until checks are green and every review thread is addressed, repairing findings as needed."
